.col-lg-offset-4.col-md-offset-3.col-sm-offset-3.col-xs-offset-1
  .col-lg-6.col-md-8.col-sm-8.col-xs-11
    = form_for [category, product] do |f|
      - if product.errors.any?
        #error_explanation
          %h4.text-center.text-muted= "#{pluralize(product.errors.count, "error")} prohibited this product from being saved:"
          %ul
            - product.errors.full_messages.each do |msg|
              %li= msg

      .form-inputs
        = f.text_field :title, class: 'form-control', placeholder: 'Write title.'
        %br/
        = f.text_area :description, class: 'form-control', placeholder: 'Write description.'
        %br/
        = f.text_field :price, class: 'form-control', placeholder: 'Specify price.'
        %br/
      .form-actions
        = f.submit 'Publish your product.', class: 'btn btn-info btn-block'
        %br/
        %br/
