.col-lg-12.col-md-12.col-sm-12.col-xs-12
  .background
    %h1.text-center Reviews Are Simple
    %h3.text-center make and browse through reviews

    .col-lg-offset-1.col-md-offset-1.col-sm-offset-0
      .col-lg-11.col-md-11.col-sm-12.col-xs-12
        .row

          - categories.each do |category|
            .col-lg-3.col-md-3.col-sm-6.col-xs-12
              = link_to category do
                .thumbnail
                  .caption
                    %h4.text-center= category.name.capitalize
                    - if current_user.try(:admin?)
                      .col-lg-offset-3.col-md-offset-3.col-sm-offset-4.col-xs-offset-3
                        %button.btn.btn-default= link_to 'Edit', edit_category_path(category)
                        %button.btn.btn-default= link_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure?' }

        - unless user_signed_in?
          .col-lg-offset-2
            .col-lg-10
              %button.btn.btn-default.btn-block= link_to 'Sign up to review and add your own items.', new_user_registration_path

        - if current_user.try(:admin?)
          .col-lg-offset-2
            .col-lg-10
              %button.btn.btn-default.btn-block= link_to 'New Category', new_category_path
